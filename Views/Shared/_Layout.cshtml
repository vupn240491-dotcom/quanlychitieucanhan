<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Quản lý chi tiêu</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <nav class="bg-white shadow-lg border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="@Url.Action("Index", "Dashboard")" class="flex items-center space-x-2">
                        <span class="text-2xl">💰</span>
                        <span class="text-xl font-bold text-gray-900">Chi tiêu cá nhân</span>
                    </a>
                </div>
                
                <div class="flex items-center space-x-4">
                    <a href="@Url.Action("Index", "Dashboard")" 
                       class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                        Trang chủ
                    </a>
                    <a href="@Url.Action("Index", "Expenses")" 
                       class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                        Chi tiêu
                    </a>
                    <a href="@Url.Action("Index", "Categories")" 
                       class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                        Danh mục
                    </a>
                    <a href="@Url.Action("Index", "Reports")" 
                       class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                        Báo cáo
                    </a>
                    <a href="@Url.Action("Index", "Profile")" 
                       class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                        Tài khoản
                    </a>
                    
                    <form method="post" action="@Url.Action("Logout", "Account")" class="inline">
                        <button type="submit" 
                                class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                            Đăng xuất
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        @RenderBody()
    </main>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>